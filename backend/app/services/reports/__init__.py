"""Report engine package.

Public API:
    :mod:`datasources`   — whitelist of tables/columns available for reporting.
    :mod:`operators`     — filter operator catalog per column type.
    :mod:`query`         — SQLAlchemy query builder driven by user filters.
    :mod:`export`        — CSV / XLSX export helpers.
"""
